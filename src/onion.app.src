{application, onion, [
    {description, "Utility library for working with the Tor network"},
    {vsn, "0.1.0"},
    {modules, []},
    {registered, []},
    {applications, [
        base32,
        crypto,
        enacl,
        gproc,
        inet_cidr,
        kernel,
        public_key,
        ssl,
        stdlib
    ]}
]}.

%% vim: set ft=erlang :
